
import { Activity, CreateActivity, Question, QuestionType } from "@/types";
import { useState } from "react";

export default function CreateActivity ({id}: {id: string} ) {
 
    
    

    return (
        <p>{id}</p>
    )

    
   
}
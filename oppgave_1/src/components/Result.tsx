import { createTasks } from "@/features/createTasks"
import { type Task } from "@/types"

const taskList = createTasks


export default function Result (/*answers: Map<Task["id"], { attempts: number, type: Task["type"]}>*/) {

    const answers = new Map<Task["id"], { attempts: number, type: Task["type"]}>()

    


}